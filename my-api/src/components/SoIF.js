import React from 'react'
import { connect } from 'react-redux'

const SoIF = () => {
  
  return (
    <div>
      <h1>SONG OF ICE AND FIRE</h1>
    </div>
  )
}


const mapStateToProps = state => ({
  
})



export default connect(mapStateToProps, {})(SoIF)
